// Package config parses Aperture's YAML configuration files, extracting
// service definitions including names, host/path regexps, pricing, and
// capabilities. Only the fields needed for event building are retained.
package config
